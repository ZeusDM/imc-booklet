Let \(n\) be a positive integer. Find all \(n\times n\) real matrices \(A\) with only real eigenvalues satisfying
\[
A + A^k = A^T
\]
for some integer \(k\ge n\).

(\(A^T\) denotes the transpose of \(A\).)
