Seja \(n\) um número inteiro positivo. Determine todas as matrizes reais \(A\) de tamanho \(n \times n\) com apenas autovalores reais que satisfazem
\[
A + A^k = A^T
\]
para algum inteiro \(k \ge n\).

(\(A^T\) denota a transposta de \(A\).)
