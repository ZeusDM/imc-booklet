Seja \(A\) uma matriz real \(n\times n\) tal que \(A^3=0\).
\begin{enumerate}[label = (\alph*)]
	\item Prove que existe uma matriz real \(n\times n\) única \(X\) que satisfaz a equação \(X+AX+XA^2=A\).
	\item Expresse \(X\) em termos de \(A\).
\end{enumerate}
