Seja \(C=\{4,6,8,9,10,\ldots\}\) o conjunto de inteiros positivos compostos. Para cada \(n\in C\), seja \(a_n\) o menor inteiro positivo \(k\) tal que \(k!\) é divisível por \(n\). Determine se a seguinte série converge:
\[\sum_{n\in C}\left(\frac{a_n}{n}\right)^n.\]
