Uma matriz complexa \(A\) de dimensão \(n \times n\) é chamada de \emph{t-normal} se \(AA^t = A^t A\), onde \(A^t\) é a transposta de \(A\). Para cada \(n\), determine a dimensão máxima de um espaço linear de matrizes complexas \(n \times n\) consistindo de matrizes t-normais.
